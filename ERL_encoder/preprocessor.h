#include <fstream>
#include <string>
#include <vector>

class preprocessor_
{
    public:
        std::ifstream iFile;
        std::string inputWord;
        std::vector<std::string> outputWords;
    private:
        
};